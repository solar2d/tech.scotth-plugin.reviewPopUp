Sources for the plugin `plugin.reviewPopUp`.

Add following to your `build.settings` to use:
```lua
{
    plugins = {
        "plugin.reviewPopUp" = {
            publisherId = "tech.scotth",
        },
    },
}
```
